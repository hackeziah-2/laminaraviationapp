
  # FLEET_MANAGEMENT

  This is a code bundle for FLEET_MANAGEMENT. The original project is available at https://www.figma.com/design/oEeyeeowVtTiNPINocB0RZ/FLEET_MANAGEMENT.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  